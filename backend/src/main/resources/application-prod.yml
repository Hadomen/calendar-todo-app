spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  sql:
    init:
      mode: always

cors:
  allowed-origins: ${FRONTEND_URL:*}
